# Core dependencies for the security runtime under src/.
#
# PINNED for the same reason as requirements-api.txt: these are the versions the
# full test suite was verified against. Bump on purpose, in a PR, with tests green.

pyyaml==6.0.3
jsonschema==4.26.0
ollama==0.6.2

# Security and validation
pathspec==1.1.1

# requests is used by http.fetch (src/tools/http_fetch.py) and the PyPI queries
# in src/tools/package_manager.py
requests==2.34.2

# gitpython>=3.1.0  # not used: src/tools/git_ops.py shells out to git directly

# MCP front door (mcp_server.py). Not a deployment dependency -- the Render
# service installs requirements-api.txt only -- but the test suite exercises the
# guarded MCP surface, so CI needs it.
mcp==1.28.0
